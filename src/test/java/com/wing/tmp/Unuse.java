package com.wing.tmp;

public class Unuse
{

        // delete queues
//        for(int i=0; i<10; i++)
//            mq.getChannel().queueDelete( String.valueOf(i) );
//        mq.getChannel().exchangeDelete( "exchange0" );

}
